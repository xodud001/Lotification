
spring.profiles.active=test

# == Database ==
db.username=admin
db.password=1234
db.url=jdbc:tc:mysql:8:///lotification

# == jpa
jpa.ddl-auto=create-drop

spring.config.import=optional:configserver:http://localhost:9999

riot.api-key=TEST_API_KEY

# need riot developer api key
kakao.grant-type=${env.grant-type}
kakao.api-key=${env.kakao-api-key}
kakao.redirect-url=${env.kakao-redirect-url}

token.secret-key=${env.secret-key}