spring.mail.host=${env.mail.host}
spring.mail.port=${env.mail.port}
spring.mail.username=${env.mail.username}
spring.mail.password=${env.mail.password}
spring.mail.properties.mail.smtp.auth=${env.mail.properties.mail.smtp.auth}
spring.mail.properties.mail.smtp.starttls.enable=${env.mail.properties.mail.smtp.starttls.enable}

spring.mail.transport.protocol=${env.mail.transport.protocol}
spring.mail.debug=${env.mail.debug}
spring.mail.default.encoding=${env.mail.default.encoding}